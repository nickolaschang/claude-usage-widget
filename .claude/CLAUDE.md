# Claude Code instructions

Everything an agent needs to install or change this project is in AGENTS.md at the repository
root, imported here so Claude Code loads it automatically when you work in this repository:

@../AGENTS.md

This file lives in `.claude/` rather than at the root because the repository root is also the root
of the Claude Code plugin, and a root `CLAUDE.md` is not loaded for plugin users anyway.
